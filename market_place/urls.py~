from django.urls import path

from . import views

urlpatterns = [
        # market place
        path('',views.get_marketplace),
        # current time
        path('time/',views.get_current_time)
        ]
